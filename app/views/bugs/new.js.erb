$('#blog-form').html("<%= j (render 'form') %>");
$('#blog-form').slideDown(0);
setTimeout(function(){ 
	if((obj = document.getElementById("submitbutton")) && obj != null){
    window.scrollTo(0, obj.offsetTop);
} }, 400);